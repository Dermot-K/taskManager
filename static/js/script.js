$(document).ready(function () {
    // sidenav
    $(".sidenav").sidenav({ edge: "right" });

    // accordion
    $('.collapsible').collapsible();

    // tooltip
    $('.tooltipped').tooltip();
});

